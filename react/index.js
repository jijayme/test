// Import React library
import React from 'react';

// Define a functional component named HelloWorld
const HelloWorld = () => {
  // Return JSX (JavaScript XML) to describe the structure of the component
  return (
    <div>
      <h1>Hello, World!</h1>
      <p>This is a simple React.js application.</p>
    </div>
  );
};

// Export the component to make it available for other parts of the application
export default HelloWorld;
